import AuthCard from "@/components/login/AuthCard"

export default function LoginPage() {
    return (
        <section className="min-h-[calc(100vh-64px)] flex items-center justify-center bg-surface p-6">
            <AuthCard />
        </section>
    )
}